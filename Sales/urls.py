from django.urls import path
from .views import *

urlpatterns = [
    # path('transactions/', transactions.as_view()),
    # path('transactions/<str:date>', transaction_list.as_view()),
    # path('transactions/add/', transaction_add.as_view()),
    # path('transactions/<int:id>/update', transaction_update.as_view()),
    # path('transactions/<int:id>/delete', transaction_delete.as_view()),
    # path('calculator/',calsulator.as_view()),
    # path('generate_bill/',generate_bill.as_view()),
]
